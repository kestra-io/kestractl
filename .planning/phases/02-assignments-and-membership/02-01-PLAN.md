---
phase: 02-assignments-and-membership
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/iam_roles.go
  - src/cli/iam_role_bindings.go
  - src/cli/iam_resolvers.go
autonomous: true

must_haves:
  truths:
    - "Operator can attach a role to a user by ID or name"
    - "Operator can detach a role from a user by ID or name"
    - "Operator can attach a role to a group by ID or name"
    - "Operator can detach a role from a group by ID or name"
  artifacts:
    - path: "src/cli/iam_resolvers.go"
      provides: "ID-or-name resolution helpers with ambiguity errors"
    - path: "src/cli/iam_role_bindings.go"
      provides: "roles bindings attach/detach commands and SDK calls"
    - path: "src/cli/iam_roles.go"
      provides: "roles bindings command wiring"
  key_links:
    - from: "src/cli/iam_roles.go"
      to: "newIamRolesBindingsCommand"
      via: "roles command AddCommand"
      pattern: "AddCommand\\(newIamRolesBindingsCommand\\(\\)\\)"
    - from: "src/cli/iam_role_bindings.go"
      to: "resolveIamRoleIdentifier"
      via: "attach/detach run functions"
      pattern: "resolveIamRoleIdentifier"
    - from: "src/cli/iam_role_bindings.go"
      to: "client.API"
      via: "role bindings SDK calls"
      pattern: "RolesAPI|UsersAPI|GroupsAPI"
---

<objective>
Add role binding attach/detach commands for users and groups.

Purpose: Enable role assignment workflows from the CLI per Phase 2 scope.
Output: `kestra iam roles bindings {attach|detach}` with ID-or-name resolution and formatted output.
</objective>

<execution_context>
@/Users/k_/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/k_/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-assignments-and-membership/02-CONTEXT.md
@src/cli/iam_roles.go
@src/cli/iam_users.go
@src/cli/iam_groups.go
@src/cli/helpers.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IAM identifier resolution helpers</name>
  <files>src/cli/iam_resolvers.go</files>
  <action>
Create `src/cli/iam_resolvers.go` with helper functions to resolve ID-or-name values into `{id, name}` pairs, returning clear errors for not found or ambiguous results.

Implement helpers:
- `resolveIamRoleIdentifier(client, value)` resolves via `RolesAPI.SearchRoles(...).Page(1).Size(1000)` and matches by `id` first, then exact `name`. If multiple name matches, return an error listing the matched IDs/names.
- `resolveIamGroupIdentifier(client, value)` resolves via `GroupsAPI.SearchGroups(...).Page(1).Size(1000)` and matches by `id` first, then exact `name`.
- `resolveIamUserIdentifier(client, value)` resolves via `UsersAPI.ListUsers(...).Page(1).Size(1000)` and matches by `id` first, then exact `username`, then exact `displayName`. If multiple matches at any stage, return an error listing the matched users (id + username + displayName).

Use `strings.TrimSpace` to reject empty inputs, and return `fmt.Errorf("<entity> not found: %s", value)` for missing matches. Keep helpers small and reuse a shared result struct where helpful.
  </action>
  <verify>go test ./...</verify>
  <done>Resolver helpers return a single resolved ID + name or a clear error for not found/ambiguous inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Wire roles bindings command group and flags</name>
  <files>src/cli/iam_roles.go, src/cli/iam_role_bindings.go</files>
  <action>
Add a `bindings` subcommand under `kestra iam roles` in `src/cli/iam_roles.go` via `newIamRolesBindingsCommand()`.
In `src/cli/iam_role_bindings.go`, define Cobra commands:
- `bindings attach` and `bindings detach`
- Required flags: `--role <id-or-name>` and exactly one of `--user <id-or-name>` or `--group <id-or-name>`

Validate output format and client creation in each command `RunE`. Enforce mutual exclusivity for `--user`/`--group` with a clear error message. Add Examples covering user and group binding cases.
  </action>
  <verify>go test ./...</verify>
  <done>CLI exposes `kestra iam roles bindings attach|detach` with required flags and validation.</done>
</task>

<task type="auto">
  <name>Task 3: Implement role binding SDK calls and output</name>
  <files>src/cli/iam_role_bindings.go</files>
  <action>
Implement attach/detach run functions to:
- Resolve role ID + name via `resolveIamRoleIdentifier`.
- Resolve target user or group via `resolveIamUserIdentifier` / `resolveIamGroupIdentifier` based on flags.
- Call the appropriate SDK endpoint for user-role or group-role bindings (use existing client patterns and `formatSDKError` on failures).

Output formatting:
- JSON output: `{action, role: {id, name}, target: {type, id, name}}`.
- Table output: columns `ACTION`, `TARGET_TYPE`, `TARGET_ID`, `TARGET_NAME`, `ROLE_ID`, `ROLE_NAME` with resolved values and `-` for missing name.

Ensure error cases align with context: not found, already assigned, and detach of missing relationship should surface SDK errors cleanly.
  </action>
  <verify>go test ./...</verify>
  <done>Attach/detach role bindings call SDK endpoints and return formatted results for user or group targets.</done>
</task>

</tasks>

<verification>
- `go test ./...`
</verification>

<success_criteria>
- `kestra iam roles bindings attach --role ops --user user@example.com` attaches role to the resolved user and outputs action + target details.
- `kestra iam roles bindings detach --role ops --group admins` detaches role from the resolved group with a confirmation output.
- Ambiguous or missing IDs/names produce clear, actionable errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-assignments-and-membership/02-01-SUMMARY.md`
</output>
