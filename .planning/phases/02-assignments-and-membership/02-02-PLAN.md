---
phase: 02-assignments-and-membership
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/cli/iam_groups.go
  - src/cli/iam_group_memberships.go
autonomous: true

must_haves:
  truths:
    - "Operator can add a user to a group by ID or name"
    - "Operator can remove a user from a group by ID or name"
  artifacts:
    - path: "src/cli/iam_group_memberships.go"
      provides: "groups attach/detach membership commands and SDK calls"
    - path: "src/cli/iam_groups.go"
      provides: "groups membership command wiring"
  key_links:
    - from: "src/cli/iam_groups.go"
      to: "newIamGroupsAttachCommand"
      via: "groups command AddCommand"
      pattern: "AddCommand\\(newIamGroupsAttachCommand\\(\\)\\)"
    - from: "src/cli/iam_group_memberships.go"
      to: "resolveIamGroupIdentifier"
      via: "attach/detach run functions"
      pattern: "resolveIamGroupIdentifier"
    - from: "src/cli/iam_group_memberships.go"
      to: "client.API"
      via: "group membership SDK calls"
      pattern: "GroupsAPI|UsersAPI"
---

<objective>
Add group membership attach/detach commands under `kestra iam groups`.

Purpose: Enable group membership management from the CLI per Phase 2 scope.
Output: `kestra iam groups {attach|detach}` with ID-or-name resolution and formatted output.
</objective>

<execution_context>
@/Users/k_/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/k_/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-assignments-and-membership/02-CONTEXT.md
@src/cli/iam_groups.go
@src/cli/iam_users.go
@src/cli/helpers.go
@src/cli/iam_resolvers.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add groups attach/detach commands and flags</name>
  <files>src/cli/iam_groups.go, src/cli/iam_group_memberships.go</files>
  <action>
Add `attach` and `detach` subcommands under `kestra iam groups` in `src/cli/iam_groups.go`.
In `src/cli/iam_group_memberships.go`, define Cobra commands with required flags:
- `--group <id-or-name>`
- `--user <id-or-name>`

Validate output format and client creation in each command `RunE`. Enforce required flags and provide examples for attach and detach workflows using name or ID for both group and user.
  </action>
  <verify>go test ./...</verify>
  <done>CLI exposes `kestra iam groups attach|detach` with required flags and validation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement group membership SDK calls and output</name>
  <files>src/cli/iam_group_memberships.go</files>
  <action>
Implement attach/detach run functions to:
- Resolve group ID + name via `resolveIamGroupIdentifier`.
- Resolve user ID + name via `resolveIamUserIdentifier`.
- Call the appropriate SDK endpoint to add/remove the user from the group (follow existing client patterns and use `formatSDKError` on failures).

Output formatting:
- JSON output: `{action, group: {id, name}, user: {id, name, displayName}}`.
- Table output: columns `ACTION`, `GROUP_ID`, `GROUP_NAME`, `USER_ID`, `USERNAME`, `DISPLAY_NAME` with resolved values and `-` for missing fields.

Surface SDK errors for not found, already member, or missing membership without swallowing details.
  </action>
  <verify>go test ./...</verify>
  <done>Group membership attach/detach calls SDK endpoints and returns formatted results.</done>
</task>

</tasks>

<verification>
- `go test ./...`
</verification>

<success_criteria>
- `kestra iam groups attach --group ops --user user@example.com` adds the resolved user to the resolved group and prints action output.
- `kestra iam groups detach --group ops --user user@example.com` removes the membership and prints a confirmation.
- Errors for missing or ambiguous identifiers are explicit and actionable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-assignments-and-membership/02-02-SUMMARY.md`
</output>
