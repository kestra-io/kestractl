---
phase: 01-iam-entities
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/cli/iam.go
  - src/cli/iam_groups.go
autonomous: true

must_haves:
  truths:
    - "Operator can create IAM groups from the CLI with required fields only"
    - "Operator can list IAM groups and see ID plus name"
    - "Operator can delete an IAM group by ID and receive a confirmation summary"
  artifacts:
    - path: "src/cli/iam.go"
      provides: "IAM command group with groups subcommand wired"
    - path: "src/cli/iam_groups.go"
      provides: "groups create/list/delete commands and SDK calls"
  key_links:
    - from: "src/cli/iam_groups.go"
      to: "client.API.GroupsAPI.CreateGroup"
      via: "create command run function"
      pattern: "GroupsAPI\\.CreateGroup"
    - from: "src/cli/iam_groups.go"
      to: "client.API.GroupsAPI.SearchGroups"
      via: "list command run function"
      pattern: "GroupsAPI\\.SearchGroups"
    - from: "src/cli/iam_groups.go"
      to: "client.API.GroupsAPI.DeleteGroup"
      via: "delete command run function"
      pattern: "GroupsAPI\\.DeleteGroup"
---

<objective>
Add IAM groups CLI commands under `kestra iam`.

Purpose: Enable group CRUD workflows from the CLI per Phase 1 scope.
Output: Groups subcommand wired to SDK with create/list/delete behavior.
</objective>

<execution_context>
@/Users/k_/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/k_/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-iam-entities/01-CONTEXT.md
@src/cli/iam.go
@src/cli/client.go
@src/cli/helpers.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add groups subcommand</name>
  <files>src/cli/iam.go, src/cli/iam_groups.go</files>
  <action>
Extend `newIamCommand()` to add a groups subcommand.
Create `src/cli/iam_groups.go` with `newIamGroupsCommand()` and subcommands:
- `create`: `kestra iam groups create --name <name>` (required) with optional `--description`.
- `list`: `kestra iam groups list` with no pagination flags.
- `delete`: `kestra iam groups delete <id>` positional ID only.

Follow existing Cobra patterns and use `validateOutputFormat` and `NewClient`.
  </action>
  <verify>go test ./...</verify>
  <done>CLI has `kestra iam groups {create|list|delete}` commands registered under iam.</done>
</task>

<task type="auto">
  <name>Task 2: Implement groups API calls and output formatting</name>
  <files>src/cli/iam_groups.go</files>
  <action>
Implement run functions with SDK calls and formatted output:
- Create: build `IAMGroupControllerApiCreateGroupRequest` with required name and optional description; call `client.API.GroupsAPI.CreateGroup(client.Ctx, client.Tenant).IAMGroupControllerApiCreateGroupRequest(req).Execute()`.
- List: call `client.API.GroupsAPI.SearchGroups(client.Ctx, client.Tenant).Page(1).Size(1000).Execute()`; output JSON array with `id`, `name`; table output includes `ID`, `Name`.
- Delete: call `client.API.GroupsAPI.DeleteGroup(client.Ctx, id, client.Tenant).Execute()`; print confirmation summary in table mode; JSON output prints `{id, deleted: true}`.

Use `formatSDKError` on errors and ensure list output includes ID + name by default.
  </action>
  <verify>go test ./...</verify>
  <done>Groups create/list/delete use SDK endpoints with JSON/table output and delete confirmation.</done>
</task>

</tasks>

<verification>
- `go test ./...`
</verification>

<success_criteria>
- `kestra iam groups create --name ops` creates a group and outputs ID + name.
- `kestra iam groups list` shows ID + name in table and JSON.
- `kestra iam groups delete <id>` deletes by ID with confirmation summary and errors when not found.
</success_criteria>

<output>
After completion, create `.planning/phases/01-iam-entities/01-03-SUMMARY.md`
</output>
