---
phase: 01-iam-entities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/root.go
  - src/cli/iam.go
  - src/cli/iam_users.go
autonomous: true

must_haves:
  truths:
    - "Operator can create IAM users from the CLI with required fields only"
    - "Operator can list IAM users and see ID plus a name field"
    - "Operator can delete an IAM user by ID and receive a confirmation summary"
  artifacts:
    - path: "src/cli/iam.go"
      provides: "IAM command group and users subcommand wiring"
    - path: "src/cli/iam_users.go"
      provides: "users create/list/delete commands and API calls"
    - path: "src/cli/root.go"
      provides: "root command registration for iam"
  key_links:
    - from: "src/cli/iam_users.go"
      to: "client.API.UsersAPI.CreateUser"
      via: "create command run function"
      pattern: "UsersAPI\\.CreateUser"
    - from: "src/cli/iam_users.go"
      to: "client.API.UsersAPI.ListUsers"
      via: "list command run function"
      pattern: "UsersAPI\\.ListUsers"
    - from: "src/cli/iam_users.go"
      to: "client.API.UsersAPI.DeleteUser"
      via: "delete command run function"
      pattern: "UsersAPI\\.DeleteUser"
    - from: "src/cli/root.go"
      to: "newIamCommand"
      via: "root command AddCommand"
      pattern: "AddCommand\\(newIamCommand\\(\\)\\)"
---

<objective>
Add IAM users CLI commands under `kestra iam`.

Purpose: Enable user CRUD workflows from the CLI per Phase 1 scope.
Output: IAM command group, users subcommand, and create/list/delete behaviors wired to the SDK.
</objective>

<execution_context>
@/Users/k_/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/k_/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-iam-entities/01-CONTEXT.md
@src/cli/root.go
@src/cli/client.go
@src/cli/helpers.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IAM command group and users subcommands</name>
  <files>src/cli/root.go, src/cli/iam.go, src/cli/iam_users.go</files>
  <action>
Create `newIamCommand()` in `src/cli/iam.go` that defines the `iam` command group and adds only the users subcommand in this plan.
Wire the new IAM command in `src/cli/root.go` with `root.AddCommand(newIamCommand())`.

In `src/cli/iam_users.go`, define `newIamUsersCommand()` with subcommands:
- `create`: `kestra iam users create --email <email>` (required) with optional `--first-name`, `--last-name`, `--password`, `--super-admin`, `--restricted`. No JSON file inputs.
- `list`: `kestra iam users list` with no pagination flags; keep output simple.
- `delete`: `kestra iam users delete <id>` positional ID only, no `--force`.

Follow existing Cobra patterns (Use/Short/Long/Example, Args validation, validateOutputFormat, NewClient).
  </action>
  <verify>go test ./...</verify>
  <done>CLI has `kestra iam users {create|list|delete}` commands and root wiring compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Implement users API calls and output formatting</name>
  <files>src/cli/iam_users.go</files>
  <action>
Implement run functions that call the SDK with `formatSDKError` on failures:
- Create: build `IAMUserControllerApiCreateOrUpdateUserRequest` with required `email` and any optional flags; call `client.API.UsersAPI.CreateUser(client.Ctx).IAMUserControllerApiCreateOrUpdateUserRequest(req).Execute()`.
- List: call `client.API.UsersAPI.ListUsers(client.Ctx).Page(1).Size(1000).Execute()` to keep list simple without pagination flags; map results to JSON array with `id`, `username`, `displayName` and print table with columns `ID`, `Username`, `DisplayName` (fallback to `-` if empty).
- Delete: call `client.API.UsersAPI.DeleteUser(client.Ctx, id).Execute()`, print a confirmation summary (ID + message) in table mode; JSON output prints `{id, deleted: true}`.

Ensure output format follows `globalFlags.Output` and table output includes ID plus a name field per decision.
  </action>
  <verify>go test ./...</verify>
  <done>Create/list/delete users use SDK endpoints with JSON/table output and delete confirmation.</done>
</task>

</tasks>

<verification>
- `go test ./...`
</verification>

<success_criteria>
- `kestra iam users create --email user@example.com` calls UsersAPI.CreateUser and outputs ID + name.
- `kestra iam users list` shows ID plus name fields in table and JSON.
- `kestra iam users delete <id>` deletes by ID with a confirmation summary and errors when not found.
</success_criteria>

<output>
After completion, create `.planning/phases/01-iam-entities/01-01-SUMMARY.md`
</output>
