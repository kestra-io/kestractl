---
phase: 01-iam-entities
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/cli/iam_users.go
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "User created via CLI appears in UI for current tenant"
    - "CLI can assign tenants and groups when creating a user"
    - "Default tenant assignment uses the CLI current tenant when none provided"
  artifacts:
    - path: "src/cli/iam_users.go"
      provides: "Create flags for tenant/group assignment and request wiring"
      contains: "SetTenants"
    - path: "src/cli/iam_users.go"
      provides: "Default tenant fallback to client tenant"
      contains: "client.Tenant"
  key_links:
    - from: "src/cli/iam_users.go"
      to: "IAMUserControllerApiCreateOrUpdateUserRequest"
      via: "SetTenants/SetGroups"
      pattern: "SetTenants|SetGroups"
    - from: "src/cli/iam_users.go"
      to: "client.Tenant"
      via: "default tenant fallback"
      pattern: "client\\.Tenant"
---

<objective>
Ensure IAM users created via CLI are assigned to a tenant (and optional groups) so they appear in the UI.

Purpose: Close the post-verification gap where CLI-created users are invisible in the UI.
Output: Updated IAM user create flags and request payload wiring.
</objective>

<execution_context>
@/Users/k_/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/k_/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/cli/iam_users.go
@src/cli/client.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tenant/group assignment flags to user create</name>
  <files>src/cli/iam_users.go</files>
  <action>
Add options for tenant and group assignments in `iamUserCreateOptions` and expose flags on `kestra iam users create`.
- Add `Tenants []string` and `Groups []string` to the options struct.
- Add repeatable flags using `StringArrayVar`:
  - `--assign-tenant` (tenant names, repeatable)
  - `--group` (group IDs, repeatable)
- Update the create command description/examples to show one example assigning a tenant and a group.
- Avoid naming conflicts with the global `--tenant` flag (do not reuse `--tenant` on the subcommand).
  </action>
  <verify>go test ./...</verify>
  <done>`kestra iam users create --help` shows `--assign-tenant` and `--group` with clear descriptions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire tenants/groups into create request with defaults</name>
  <files>src/cli/iam_users.go</files>
  <action>
Set the API request `Tenants` and `Groups` fields so UI scoping works.
- If `opts.Tenants` is non-empty, set `req.SetTenants(opts.Tenants)`.
- If `opts.Tenants` is empty, default to the current CLI tenant: `req.SetTenants([]string{client.Tenant})`.
- If `opts.Groups` is non-empty, set `req.SetGroups(opts.Groups)`.
- Do not change existing behavior for names/password/admin flags; only add tenant/group wiring.
  </action>
  <verify>go test ./...</verify>
  <done>Creating a user sends tenants/groups in the request, defaulting to the current tenant when none are supplied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>IAM user creation now assigns tenants/groups and defaults to the current tenant.</what-built>
  <how-to-verify>
    1. Run `kestra iam users create --email ui-check@example.com` (optionally add `--assign-tenant <tenant>` and `--group <groupId>`).
    2. Open the Kestra UI for the same tenant and visit the IAM users list.
    3. Confirm the new user appears in the list.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issue you see.</resume-signal>
</task>

</tasks>

<verification>
- `go test ./...`
- CLI help for `kestra iam users create` shows `--assign-tenant` and `--group`
</verification>

<success_criteria>
- Users created with `kestra iam users create` appear in the UI for the current tenant.
- Tenant/group assignment flags exist and correctly map to the API request.
</success_criteria>

<output>
After completion, create `.planning/phases/01-iam-entities/01-04-SUMMARY.md`
</output>
