---
phase: 01-iam-entities
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/cli/iam.go
  - src/cli/iam_roles.go
  - src/cli/iam_role_permissions.go
autonomous: true

must_haves:
  truths:
    - "Operator can create IAM roles with required permissions and name fields"
    - "Operator can list IAM roles and see ID plus name"
    - "Operator can delete an IAM role by ID and receive a confirmation summary"
  artifacts:
    - path: "src/cli/iam.go"
      provides: "IAM command group with roles subcommand wired"
    - path: "src/cli/iam_roles.go"
      provides: "roles create/list/delete commands and SDK calls"
    - path: "src/cli/iam_role_permissions.go"
      provides: "role permissions flag parsing"
  key_links:
    - from: "src/cli/iam_roles.go"
      to: "client.API.RolesAPI.CreateRole"
      via: "create command run function"
      pattern: "RolesAPI\\.CreateRole"
    - from: "src/cli/iam_roles.go"
      to: "client.API.RolesAPI.SearchRoles"
      via: "list command run function"
      pattern: "RolesAPI\\.SearchRoles"
    - from: "src/cli/iam_roles.go"
      to: "client.API.RolesAPI.DeleteRole"
      via: "delete command run function"
      pattern: "RolesAPI\\.DeleteRole"
---

<objective>
Add IAM roles CLI commands under `kestra iam`.

Purpose: Enable role CRUD workflows with explicit permissions handling.
Output: Roles subcommand wired to SDK, including permissions parsing from flags.
</objective>

<execution_context>
@/Users/k_/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/k_/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-iam-entities/01-CONTEXT.md
@src/cli/iam.go
@src/cli/client.go
@src/cli/helpers.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add roles subcommand and permissions parsing</name>
  <files>src/cli/iam.go, src/cli/iam_roles.go, src/cli/iam_role_permissions.go</files>
  <action>
Extend `newIamCommand()` to add a roles subcommand.
Create `src/cli/iam_roles.go` with `newIamRolesCommand()` and subcommands:
- `create`: `kestra iam roles create --name <name> --permission <RESOURCE:ACTION>` (repeatable). Optional `--description`, `--default`.
- `list`: `kestra iam roles list` with no pagination flags.
- `delete`: `kestra iam roles delete <id>` positional ID only.

Create `src/cli/iam_role_permissions.go` with a parser that:
- Accepts repeated `--permission` values in `RESOURCE:ACTION` format (e.g., `FLOW:READ`).
- Validates resource keys against the SDK permission struct fields: FLOW, BLUEPRINT, TEMPLATE, NAMESPACE, EXECUTION, USER, GROUP, ROLE, BINDING, AUDITLOG, SECRET, KVSTORE, IMPERSONATE, SETTING, APP, ASSET, APPEXECUTION, TEST, DASHBOARD, TENANT_ACCESS, SERVICE_ACCOUNT, INVITATION, GROUP_MEMBERSHIP.
- Aggregates actions per resource into `IAMRoleControllerApiRoleCreateOrUpdateRequestPermissions`.
- Returns a clear error for invalid formats or unknown resources.

Follow existing Cobra patterns and use `validateOutputFormat` and `NewClient`.
  </action>
  <verify>go test ./...</verify>
  <done>CLI has `kestra iam roles {create|list|delete}` commands and permissions parsing helper.</done>
</task>

<task type="auto">
  <name>Task 2: Implement roles API calls and output formatting</name>
  <files>src/cli/iam_roles.go</files>
  <action>
Implement run functions with SDK calls and formatted output:
- Create: build `IAMRoleControllerApiRoleCreateOrUpdateRequest` using parsed permissions and name; call `client.API.RolesAPI.CreateRole(client.Ctx, client.Tenant).IAMRoleControllerApiRoleCreateOrUpdateRequest(req).Execute()`.
- List: call `client.API.RolesAPI.SearchRoles(client.Ctx, client.Tenant).Page(1).Size(1000).Execute()`; output JSON array with `id`, `name`, `isDefault`, `isManaged`; table output includes `ID`, `Name`, `Default`, `Managed`.
- Delete: call `client.API.RolesAPI.DeleteRole(client.Ctx, id, client.Tenant).Execute()`; print confirmation summary in table mode; JSON output prints `{id, deleted: true}`.

Use `formatSDKError` on errors and keep list output aligned with the phase decision to show ID + name by default.
  </action>
  <verify>go test ./...</verify>
  <done>Roles create/list/delete use SDK endpoints with JSON/table output and delete confirmation.</done>
</task>

</tasks>

<verification>
- `go test ./...`
</verification>

<success_criteria>
- `kestra iam roles create --name ops --permission FLOW:READ` creates a role with permissions and outputs ID + name.
- `kestra iam roles list` shows ID + name (and default/managed) in table and JSON.
- `kestra iam roles delete <id>` deletes by ID with confirmation summary and errors when not found.
</success_criteria>

<output>
After completion, create `.planning/phases/01-iam-entities/01-02-SUMMARY.md`
</output>
